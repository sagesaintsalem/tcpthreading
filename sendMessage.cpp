#include "allheader.h"
#include "Peer.h"

void sendMessage(string message) {
	//while (true) {
	//	boost::system::error_code error;
	//	cout << "\n" << peer.name << ": ";
	//	std::getline(cin, message);
	//	clientMessage = "\n" + peer.name + ": " + message;
	//	boost::asio::write(peer.socket, boost::asio::buffer(message), error);
	//	//cout << "\n";
	//	//Back to receiving mode
	//	break;
	//}
}